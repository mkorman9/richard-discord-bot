Zleca Raider.IO pobranie aktualnych informacji o postaci.

`!skan <postać>` gdzie jako `<postać>` można podać alias (zobacz `!alias`) lub pełną nazwę postaci (np. `Jajara-Burning Legion`)

Uwaga! Jeśli pełna nazwa postaci zawiera spacje, należy ją podać w cudzysłowie.
