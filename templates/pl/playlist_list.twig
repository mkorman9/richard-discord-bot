{% if playlist.streams is empty %}
Playlista jest pusta.
{% else %}
Playlista:
{% for stream in playlist.streams %}
{% if loop.first %}
- {{ stream.title }} ({{ stream.url }}) <--- Teraz gra
{% else %}
- {{ stream.title }} ({{ stream.url }})
{% endif %}
{% endfor %}
{% endif %}
