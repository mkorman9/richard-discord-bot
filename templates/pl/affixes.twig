Affixy w tym tygodniu (początek {{ rotation.week.weekStart.format('YYYY-MM-DD') }}):
{% for affix in rotation.affixes %}
- {{ affix.name }} ({{ affix.description }})
{% endfor %}

{{ now.format('YYYY-MM-DD HH:mm Z') }}, numer tygodnia = {{ rotation.week.weekNumber }}
